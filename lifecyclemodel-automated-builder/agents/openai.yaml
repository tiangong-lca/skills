interface:
  display_name: "Lifecycle Model Automated Builder"
  short_description: "Assemble local lifecyclemodels from process runs"
  default_prompt: "Use $lifecyclemodel-automated-builder to assemble, validate, or publish-build local lifecyclemodel json_ordered artifacts from existing process build runs through the TianGong CLI."
