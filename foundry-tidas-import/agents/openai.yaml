interface:
  display_name: "TianGong Foundry"
  short_description: "Import LCA packages and develop TIDAS data from evidence"
  default_prompt: "Use $foundry-tidas-import with its bundled C1 bootstrap and adjacent trusted release lock to continue the requested TianGong Foundry workflow through current public task actions, evidence-backed semantic work, explicitly selected reference snapshots and separately authorized native execution intent when supported. Preserve current task/account bindings and original-attempt readback; never replace consumed evidence or treat a reference review or execution contract as write permission."
