interface:
  display_name: "Flow Hybrid Search"
  short_description: "Debug and tune flow hybrid search requests"
  default_prompt: "Use $flow-hybrid-search through the pinned CLI 0.1.13 OAuth session and its zero-config official Production profile; check auth status first, hand browser login to the human when required, and never request credentials, codes, or tokens."
