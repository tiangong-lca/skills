interface:
  display_name: "Lifecycle Model Hybrid Search"
  short_description: "Debug and tune lifecycle model search requests"
  default_prompt: "Use $lifecyclemodel-hybrid-search through the pinned CLI 0.1.11 OAuth session and its zero-config official Production profile; check auth status first, hand browser login to the human when required, and never request credentials, codes, or tokens."
