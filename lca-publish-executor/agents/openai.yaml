interface:
  display_name: "LCA Publish Executor"
  short_description: "Run unified publish contracts"
  default_prompt: "Use $lca-publish-executor to run a unified tiangong publish request for lifecyclemodel/process/source artifact bundles, relations, or process-build runs."
