interface:
  display_name: "LCA Publish Executor"
  short_description: "Publish LCA artifact bundles"
  default_prompt: "Use $lca-publish-executor to normalize one publish request and execute it through the unified tiangong publish contract."
