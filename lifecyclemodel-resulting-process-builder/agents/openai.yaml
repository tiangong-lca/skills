interface:
  display_name: "Lifecycle Model Resulting Process Builder"
  short_description: "Build resulting processes from lifecycle models"
  default_prompt: "Use $lifecyclemodel-resulting-process-builder to build deterministic local resulting-process datasets from a lifecycle model json_ordered file, along with relation metadata and a publish handoff bundle."
